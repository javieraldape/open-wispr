## Shortcut fixes

OpenWispr now saves shortcut changes more reliably when using the HandyKeys
keyboard backend. The shortcut recorder also supports using the `fn` key by
itself, which makes it easier to use the dedicated dictation-style key on Mac
keyboards.

This update keeps the existing shortcut active while you edit, restores it if
you cancel, and persists the new shortcut after capture completes.
